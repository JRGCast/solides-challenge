import { IUserLogin } from "src/app/data/types/user/user.model";

/**For solely the purpose of the front end development, this will act like the database */
export const mockUserList: IUserLogin[] = [
    {email: 'test@g.com', password: '123456'}
]